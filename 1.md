仓库当前在最新一次整体修复提交上（`a93513f`，P4 清理收尾）。我来实际跑一遍验证：安装依赖 → typecheck → build → 全量测试，看看现在整体健康状况。

依赖还没装。先安装依赖（项目用 pnpm lockfile）：

发现第一个问题：上一轮修复从 `package.json` 移除了 ink 相关依赖，但 **`pnpm-lock.yaml` 没有同步更新**，`--frozen-lockfile`（CI 常用模式）会直接失败。先看下 CI 怎么写的，再重新生成 lockfile 并安装：

CI 用的正是 `--frozen-lockfile`，所以这个 lockfile 不同步会直接挂掉 CI。我先重新生成 lockfile 并安装依赖，然后跑完整验证：

依赖装好了。现在跑 typecheck 和 build：

typecheck ✅ 0 错误，build ✅。现在跑全量测试（1300+ 用例，需要几分钟）：

1325/1327 通过，2 个失败 —— 提交信息说是 message-viewport 的既有失败。我确认一下具体是哪两个、是什么原因：

两个失败都在 `test/message-viewport.test.ts`。我看一下是测试断言过时还是真 bug：

两个失败都是「宽表格视口高度」相关的断言。深入看一下 `message-viewport.ts` 的实现，判断是回归还是测试过时：

现在明白关键了：`estimateMessageHeight`（估算）如今也用 `countMarkdownRenderRows`（与实际渲染同行数口径），两者已收敛。要判断测试过时还是真回归，需要确认渲染器对宽表格实际画几行：

渲染器确认：表格按「每源行一行、列对齐、超宽截断」绘制，与 `countMarkdownRenderRows` 的口径一致。再确认渲染行不会被换行撑高（否则才是真低估）：

主渲染路径会对正文做 `wrapRenderLine`（按宽度换行），这与 `countMarkdownRenderRows`「表格一行」的假设可能冲突。看下表格在解析时是否已被规范化/截断：

关键分歧点浮现：表格被解析成按内容对齐的宽行，而 ANSI 渲染路径会对正文按宽度**换行**（不是截断）。确认现在实际用哪个渲染器（Ink 已在本轮移除）：

`src/tui/message-viewport.ts` 已经**没有任何生产代码引用**——只剩它自己的测试和一个 bench 脚本在用。查一下 P4 规格确认这是有意保留还是漏删：

P4 规格里明确写了这 2 个失败「out of P4 scope, pre-existing」——是被有意搁置的。最后把 CI 的其余步骤也验证一遍（generate-models:check + 产物冒烟）：

全链路验证完成。把 lockfile 修复提交到当前分支并推送：

# 整体状态报告

我在最新提交 `a93513f`（P4 清理收尾）上做了一次完整的 CI 等价验证。**结论：主体已经很健康，但还留了一个会让 CI 直接挂掉的小尾巴（已修复）和 2 个“僵尸测试”（待你决策）。**

## ✅ 验证结果

| 检查项 | 结果 |
|---|---|
| `pnpm install --frozen-lockfile` | ❌ → **已修复** ✅（见下文） |
| `typecheck`（tsc --noEmit） | ✅ 0 错误 |
| `build`（vite，4 个入口产物） | ✅ 通过 |
| 测试（239 suites / 1327 用例，68s） | ✅ 1325 通过 / ❌ 2 失败 |
| `generate-models:check`（37 providers） | ✅ 目录最新 |
| `dist/cli.js` 冒烟启动 | ✅ 正常加载 skills/config，无 API key 时优雅报错 |

## 🔧 本轮发现并已修复：lockfile 不同步（会导致 CI 红灯）

上一轮 P4 从 `package.json` 移除了 `ink` / `ink-spinner` / `ink-text-input`，**但 `pnpm-lock.yaml` 没有同步重新生成**。而 CI（`.github/workflows/ci.yml`）用的是 `pnpm install --frozen-lockfile`，会直接报 `ERR_PNPM_OUTDATED_LOCKFILE` 挂掉。我已重新生成 lockfile（纯删除 278 行 ink 相关条目），提交为 `0fc53c0` 并推送到当前分支。

## 🧟 剩余 2 个测试失败：过时测试，不是产品回归

失败都在 `test/message-viewport.test.ts`，我深挖后确认了根因链条：

1. **该模块已是死代码**：`src/tui/message-viewport.ts`（Ink 时代的固定视口切片器）在 P0–P4 headless kernel 重构后，**没有任何生产代码再引用它**——现在的 TUI 走 `buildTerminalRenderLines` + `IncrementalTerminalRenderer`（scrollback 渲染）。只剩它自己的测试和一个 bench 脚本在 import 它。
2. **测试断言的是已被取代的中间态**：这两个测试验证的是“估算器对宽表格多算行数，`estimateViewportActualHeight` 负责修正”。但后来 `estimateMessageHeight` 已直接改用 `countMarkdownRenderRows`（源码注释："Mirror countMarkdownRenderRows so scroll window and drawn rows use the same row space"），估算与实际口径统一后 `actual == estimated`，`actual < estimated` 的断言自然为假。
3. P3/P4 规格里也明确记录这 2 个失败是「pre-existing, out of P4 scope」——上一轮是有意搁置的。

## 建议的收尾动作（二选一）

- **方案 A（推荐，与 P4 清理方向一致）**：删除死模块 `src/tui/message-viewport.ts` + 它的测试 + `test/tui-viewport-bench.ts`（bench 也只依赖它），直接清零失败用例，还能减负约 600+ 行。
- **方案 B（保留为库代码）**：把 2 个断言改成新不变量（`actual <= estimated`、`actualHeight <= visibleHeight`）。

要我直接执行方案 A 吗？